enum XAligns {
  left = "left",
  right = "right",
}

enum YAligns {
  top = "top",
  bottom = "bottom",
}

export const parseAlign = (align: string): `${XAligns} ${YAligns}` => {
  const findedXAlign = align.
  return "left top";
};
